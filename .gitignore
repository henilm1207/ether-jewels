node_modules/
dist/
.env
.DS_Store
*.log
# Local product uploads live on the VPS disk, never in git.
server/public/uploads/*
!server/public/uploads/.gitkeep
# Migration backups hold production data snapshots — never commit.
server/*-migration-backup.json
